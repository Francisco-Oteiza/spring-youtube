server:
  port: 8080

spring:
  datasource:
    url: jdbc:postgresql://db:5432/youtube
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: update
    open-in-view: false
